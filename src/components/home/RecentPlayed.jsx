import EmptyState from './EmptyState'; export default function RecentPlayed({games,onClear}){return <section className='glass p-4'><div className='flex justify-between'><h3>เล่นล่าสุด</h3><button onClick={onClear}>ล้าง</button></div>{games.length? <div className='flex gap-2 mt-2 flex-wrap'>{games.map(g=><span key={g.id} className='glass px-2 py-1'>{g.icon} {g.title}</span>)}</div>:<EmptyState text='ยังไม่มีรายการล่าสุด'/>}</section>}
