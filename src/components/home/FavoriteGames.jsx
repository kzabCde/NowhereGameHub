import EmptyState from './EmptyState';
export default function FavoriteGames({games}){return <section className='surface-card p-4'><h3 className='text-sm uppercase tracking-[0.2em] text-muted'>เกมโปรด</h3>{games.length?<div className='mt-2 flex flex-wrap gap-2'>{games.map(g=><span key={g.id} className='surface-panel px-3 py-1 text-sm'>{g.icon} {g.title}</span>)}</div>:<EmptyState text='ยังไม่มีเกมโปรด'/>}</section>}
