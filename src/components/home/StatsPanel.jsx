export default function StatsPanel({total,available,favorites,recent}){return <section className='grid grid-cols-2 gap-3 md:grid-cols-4'>{[['ทั้งหมด',total],['พร้อมเล่น',available],['รายการโปรด',favorites],['ล่าสุด',recent]].map(([l,v])=><div key={l} className='metal-card p-4'><p className='font-hud text-[10px] uppercase tracking-[0.2em] text-zinc-500 dark:text-zinc-400'>{l}</p><p className='font-hud text-3xl font-black'>{v}</p></div>)}</section>}
